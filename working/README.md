# Generated outputs

- **`submission.csv`** — model predictions for upload (default write target for `python -m golf_ft.infer`).

This matches the platform expectation of `./working/submission.csv`. Regenerated by training/inference; not part of the organizer zip.
